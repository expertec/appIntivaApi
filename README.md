# appIntiva API

Este repositorio ahora contiene exclusivamente el backend (Node.js/Express) que se despliega en Render.

## Scripts
- `npm install`
- `npm run dev`
- `npm start`

El antiguo frontend vive localmente en `client/` y está excluido del repositorio para evitar que Render ejecute la build de React.
